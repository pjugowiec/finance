databaseChangeLog:
  - changeSet:
      id: create-balance_transaction-table
      author: Patryk Jugowiec
      changes:
        - createSequence:
            catalogName: seq
            cycle: true
            incrementBy: 1
            maxValue: 1000000
            minValue: 1
            schemaName: public
            sequenceName: balance_transaction_seq
            startValue: 1
        - createTable:
            tableName: balance_transactions
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: description
                  type: varchar(500)
              - column:
                  name: balance
                  type: bigint
                  constraints:
                    nullable: false
              - column:
                  name: category_id
                  type: bigint
                  constraints:
                    nullable: false
                    foreignKeyName: fk_category_balance_transactions
                    references: dictionaries(id)
              - column:
                  name: balance_flow_id
                  type: bigint
                  constraints:
                    nullable: false
                    foreignKeyName: fk_balance_flow_balance_transactions
                    references: dictionaries(id)
              - column:
                  name: created
                  type: date
                  constraints:
                      nullable: false
              - column:
                  name: currency_id
                  type: bigint
                  constraints:
                    nullable: false
                    foreignKeyName: fk_currency_balance_transactions
                    references: currencies(id)
              - column:
                  name: user_id
                  type: bigint
                  constraints:
                    nullable: false
                    foreignKeyName: fk_user_balance_transactions
                    references: _adm_users(id)




